{
  "name": "momi-toast-ingest",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "node --test tests/verify_invalid_toast_signature.test.ts tests/verify_valid_toast_signature.test.ts"
  }
}
