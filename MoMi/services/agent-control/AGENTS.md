# Retired agent-control tombstone

- This directory is historical metadata only.
- Do not add runtime code, functions, tests, deploy configuration, contracts,
  network access, secrets, or new migrations here.
- `thedoughmonster/momi-symphony` on `main` is the sole executable and future
  schema authority.
- Preserve the owned-dataset inventory only so this repository can validate
  its immutable applied migration history.
