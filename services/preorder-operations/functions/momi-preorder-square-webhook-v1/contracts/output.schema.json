{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["ok", "disposition"],
  "properties": {
    "ok": { "const": true },
    "disposition": { "enum": ["applied", "duplicate", "stale", "conflict", "mismatch", "missing", "indeterminate", "unmatched", "ignored"] }
  }
}
