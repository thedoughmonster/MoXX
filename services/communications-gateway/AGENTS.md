# Communications Gateway Rules

- Admit only authenticated exact-email beta users and the `momi-assistant` alias.
- Enforce access, idempotency, and every effective user limit before model execution.
- Never retry an ambiguous paid attempt automatically.
- Use only manifest-declared versioned owner contracts for archive, tools, and logs.
- Archive complete ordered model-visible evidence before advancing or succeeding.
- Never persist or log credentials, authorization headers, or protected values.
- Call models only through the declared model-execution-gateway contracts.
- Keep routes, provider bindings, and cohorts disabled until authorized activation.
